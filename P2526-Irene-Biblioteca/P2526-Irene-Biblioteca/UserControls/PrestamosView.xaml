<UserControl x:Class="P2526_Irene_Biblioteca.UserControls.PrestamosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <UserControl.Resources>

        <Style x:Key="InfoChip" TargetType="Border">
            <Setter Property="CornerRadius" Value="999"/>
            <Setter Property="Padding" Value="10,4"/>
            <Setter Property="Background" Value="#E8F0FE"/>
        </Style>

        <Style x:Key="ModernDataGrid" TargetType="DataGrid">
            <Setter Property="FontFamily" Value="{StaticResource AppFont}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#D7E6FF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowHeight" Value="40"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserResizeRows" Value="False"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="AlternatingRowBackground" Value="#F7FAFF"/>
            <Setter Property="HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
        </Style>

        <Style x:Key="ModernDataGridColumnHeader" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="{StaticResource BrushBlue}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="{StaticResource AppFont}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="44"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <Style x:Key="ModernDataGridRow" TargetType="DataGridRow">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E8F0FE"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#D7E6FF"/>
                    <Setter Property="Foreground" Value="#111827"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ModernDataGridCell" TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Style="{StaticResource Card}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Préstamos" Style="{StaticResource H1}"/>
                </Grid>

                <Grid Grid.Row="1" Margin="0,12,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1.4*"/>
                        <ColumnDefinition Width="1.4*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,14,0">
                        <TextBlock Text="Libro" Style="{StaticResource Label}"/>
                        <ComboBox Style="{StaticResource Combo}"
                                  ItemsSource="{Binding Libros}"
                                  DisplayMemberPath="Titulo"
                                  SelectedValuePath="IdLibro"
                                  SelectedValue="{Binding LibroId}"
                                  IsEnabled="{Binding PuedeEditar}"/>

                        <TextBlock Text="Cliente" Style="{StaticResource Label}"/>
                        <ComboBox Style="{StaticResource Combo}"
                                  ItemsSource="{Binding Clientes}"
                                  DisplayMemberPath="Nombre"
                                  SelectedValuePath="IdCliente"
                                  SelectedValue="{Binding ClienteId}"
                                  IsEnabled="{Binding PuedeEditar}"/>

                        <CheckBox Margin="0,8,0,0"
                                  FontFamily="{StaticResource AppFont}"
                                  Content="Marcar como devuelto"
                                  IsChecked="{Binding Devuelto}"
                                  IsEnabled="{Binding PuedeEditar}"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1">
                        <TextBlock Text="Empleado" Style="{StaticResource Label}"/>
                        <ComboBox Style="{StaticResource Combo}"
                                  ItemsSource="{Binding Empleados}"
                                  DisplayMemberPath="Nombre"
                                  SelectedValuePath="IdEmpleado"
                                  SelectedValue="{Binding EmpleadoId}"
                                  IsEnabled="{Binding PuedeEditar}"/>

                        <TextBlock Text="Fecha préstamo" Style="{StaticResource Label}"/>
                        <DatePicker Height="38"
                                    SelectedDate="{Binding FechaPrestamo}"
                                    IsEnabled="{Binding PuedeEditar}"/>

                        <TextBlock Text="Fecha devolución (opcional)" Style="{StaticResource Label}"/>
                        <DatePicker Height="38"
                                    SelectedDate="{Binding FechaDevolucion}"
                                    IsEnabled="{Binding PuedeEditar}"/>

                        <TextBlock Text="{Binding Mensaje}"
                                   Foreground="#DC2626"
                                   Margin="0,12,0,0"
                                   FontFamily="{StaticResource AppFont}"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2" VerticalAlignment="Center" Margin="16,0,0,0">
                        <Button Content="Añadir" Style="{StaticResource PrimaryBtn}" Click="BtnAdd_Click"
                                IsEnabled="{Binding PuedeEditar}"/>
                        <Button Content="Eliminar" Style="{StaticResource DangerBtn}" Margin="0,10,0,0" Click="BtnDelete_Click"
                                IsEnabled="{Binding PuedeEditar}"/>
                        <Button Content="Modificar" Style="{StaticResource GhostBtn}" Margin="0,10,0,0" Click="BtnUpdate_Click"
                                IsEnabled="{Binding PuedeEditar}"/>
                        <Button Content="Limpiar" Style="{StaticResource GhostBtn}" Margin="0,10,0,0" Click="BtnClear_Click"/>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource Card}" Margin="0,12,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Histórico"
                               FontFamily="{StaticResource AppFont}"
                               FontSize="16"
                               FontWeight="Bold"
                               Foreground="#111827"/>
                </Grid>

                <Border Grid.Row="1" CornerRadius="14" BorderBrush="#D7E6FF" BorderThickness="1" Margin="0,10,0,0">
                    <DataGrid Style="{StaticResource ModernDataGrid}"
                              ColumnHeaderStyle="{StaticResource ModernDataGridColumnHeader}"
                              RowStyle="{StaticResource ModernDataGridRow}"
                              CellStyle="{StaticResource ModernDataGridCell}"
                              ItemsSource="{Binding Prestamos}"
                              SelectedItem="{Binding PrestamoSeleccionado}"
                              AutoGenerateColumns="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding IdPrestamo}" Width="70"/>
                            <DataGridTextColumn Header="Libro" Binding="{Binding IdLibro}" Width="90"/>
                            <DataGridTextColumn Header="Cliente" Binding="{Binding IdCliente}" Width="90"/>
                            <DataGridTextColumn Header="Empleado" Binding="{Binding IdEmpleado}" Width="90"/>
                            <DataGridTextColumn Header="F. Préstamo" Binding="{Binding FechaPrestamo}" Width="160"/>
                            <DataGridTextColumn Header="F. Devolución" Binding="{Binding FechaDevolucion}" Width="170"/>
                            <DataGridCheckBoxColumn Header="Devuelto" Binding="{Binding Devuelto}" Width="90"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>
